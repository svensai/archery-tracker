# Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Web server
flask>=3.0.0
flask-cors>=4.0.0
flask-swagger-ui>=4.11
gunicorn>=21.2

# Database
# SQLite is built into Python
# PostgreSQL (optional — only needed when DATABASE_URL is set)
psycopg2-binary>=2.9

# Date parsing
python-dateutil>=2.8.0

# Scheduling
apscheduler>=3.10.0

# Authentication
flask-login>=0.6
flask-mail>=0.10
bcrypt>=4.0
flask-limiter>=3.5

# Testing
pytest>=8.0.0
